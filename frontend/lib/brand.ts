// Single source of truth for product naming/copy that isn't domain-specific.
// V1 only ships a health-inspection data source, but the product itself
// isn't named or described around that — it's expected to expand to other
// signal sources later, so branding stays generic to "leads." Marketing
// copy also deliberately never names *how* signals are detected (that
// mechanism is the actual product moat) — only that they're timely and real.
export const BRAND_NAME = "Axidort";
export const BRAND_TAGLINE = "Know who needs you — before anyone else does.";
